module.exports = {
  // webpack: (config, { buildId, dev, isServer, defaultLoaders, webpack }) => {
  //   return config;
  // },
  images: {
    domains: ["pearl-tu-portfolio.s3.amazonaws.com"],
  },
};
